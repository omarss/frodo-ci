apiVersion: apps/v1
kind: Deployment
metadata:
  name: cards
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cards
  template:
    metadata:
      labels:
        app: cards
    spec:
      containers:
        - name: cards
          image: cards:latest
